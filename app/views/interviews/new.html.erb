<h1>New Interview</h1>

<%= form_for :interview, url: interviews_path do |f| %>
  <%= render 'form' , f: f %>
  <p>
    <%= f.submit "Create Interview" %>
  </p>
<% end %>

<br>
<%= link_to "Back", interviews_path %>